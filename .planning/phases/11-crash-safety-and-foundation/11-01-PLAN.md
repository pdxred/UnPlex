---
phase: 11-crash-safety-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SimPlex/components/widgets/LoadingSpinner.xml
  - SimPlex/components/widgets/LoadingSpinner.brs
  - SimPlex/components/screens/HomeScreen.xml
  - SimPlex/components/screens/HomeScreen.brs
  - SimPlex/components/screens/DetailScreen.brs
  - SimPlex/components/screens/EpisodeScreen.brs
  - SimPlex/components/screens/PlaylistScreen.brs
  - SimPlex/components/screens/SearchScreen.brs
  - SimPlex/components/screens/SettingsScreen.brs
  - SimPlex/components/screens/UserPickerScreen.brs
  - SimPlex/components/widgets/VideoPlayer.xml
  - SimPlex/components/widgets/VideoPlayer.brs
  - SimPlex/source/normalizers.brs
  - SimPlex/source/capabilities.brs
autonomous: false
requirements:
  - SAFE-01
  - SAFE-02

must_haves:
  truths:
    - "BusySpinner SIGSEGV root cause is confirmed and documented"
    - "All screens use a safe loading indicator (no BusySpinner in scene graph)"
    - "The app compiles and sideloads cleanly with normalizers.brs and capabilities.brs deleted"
    - "VideoPlayer transcodingSpinner is assessed and replaced if it also crashes"
  artifacts:
    - path: "SimPlex/components/widgets/LoadingSpinner.xml"
      provides: "Safe loading spinner widget (BusySpinner replaced with Label+Rectangle overlay)"
      contains: "Label"
    - path: "SimPlex/components/widgets/LoadingSpinner.brs"
      provides: "Loading spinner logic with 300ms delay threshold and input blocking"
      contains: "onShowSpinnerChange"
  key_links:
    - from: "SimPlex/components/screens/HomeScreen.xml"
      to: "LoadingSpinner"
      via: "XML child node instantiation"
      pattern: "LoadingSpinner"
    - from: "SimPlex/components/screens/HomeScreen.brs"
      to: "LoadingSpinner"
      via: "m.loadingSpinner = m.top.findNode"
      pattern: "m\\.loadingSpinner.*findNode"
---

<objective>
Confirm BusySpinner SIGSEGV root cause, replace the crashed component with a safe loading indicator, assess VideoPlayer's transcodingSpinner, and delete confirmed orphaned files.

Purpose: This is the crash safety gate for all v1.1 work. No screen changes are safe until the SIGSEGV root cause is confirmed and a safe loading pattern is established.
Output: Safe LoadingSpinner widget, crash root cause documented, orphaned files deleted.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-crash-safety-and-foundation/11-CONTEXT.md
@.planning/phases/11-crash-safety-and-foundation/11-RESEARCH.md
@.planning/UAT-DEBUG-CONTEXT.md

@SimPlex/components/widgets/LoadingSpinner.xml
@SimPlex/components/widgets/LoadingSpinner.brs
@SimPlex/components/screens/HomeScreen.xml
@SimPlex/components/screens/HomeScreen.brs
@SimPlex/components/widgets/VideoPlayer.xml
@SimPlex/components/widgets/VideoPlayer.brs
@SimPlex/source/normalizers.brs
@SimPlex/source/capabilities.brs

<interfaces>
<!-- Current LoadingSpinner widget interface (to be preserved during replacement) -->
From SimPlex/components/widgets/LoadingSpinner.xml:
```xml
<interface>
    <field id="showSpinner" type="boolean" value="false" onChange="onShowSpinnerChange" />
</interface>
```

<!-- All 7 screen .brs files use this guard pattern — it must continue to work after replacement -->
```brightscript
' Current pattern (spinner disabled):
m.loadingSpinner = invalid ' LoadingSpinner removed - causes firmware SIGSEGV crash

' Target pattern (spinner restored with safe implementation):
m.loadingSpinner = m.top.findNode("loadingSpinner")

' Guard pattern used at ~40 call sites across all screens (DO NOT change these):
if m.loadingSpinner <> invalid then m.loadingSpinner.showSpinner = true
if m.loadingSpinner <> invalid then m.loadingSpinner.showSpinner = false
```

<!-- VideoPlayer transcodingSpinner (separate BusySpinner usage to assess) -->
From SimPlex/components/widgets/VideoPlayer.xml line 49:
```xml
<BusySpinner id="transcodingSpinner" ... />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace BusySpinner with safe loading indicator and assess VideoPlayer spinner</name>
  <files>
    SimPlex/components/widgets/LoadingSpinner.xml
    SimPlex/components/widgets/LoadingSpinner.brs
    SimPlex/components/screens/HomeScreen.xml
    SimPlex/components/screens/HomeScreen.brs
    SimPlex/components/screens/DetailScreen.brs
    SimPlex/components/screens/DetailScreen.xml
    SimPlex/components/screens/EpisodeScreen.brs
    SimPlex/components/screens/EpisodeScreen.xml
    SimPlex/components/screens/PlaylistScreen.brs
    SimPlex/components/screens/PlaylistScreen.xml
    SimPlex/components/screens/SearchScreen.brs
    SimPlex/components/screens/SearchScreen.xml
    SimPlex/components/screens/SettingsScreen.brs
    SimPlex/components/screens/SettingsScreen.xml
    SimPlex/components/screens/UserPickerScreen.brs
    SimPlex/components/screens/UserPickerScreen.xml
    SimPlex/components/widgets/VideoPlayer.xml
    SimPlex/components/widgets/VideoPlayer.brs
  </files>
  <action>
    CRITICAL FIRST STEP: Validate assumptions against the current codebase before making any changes. Read all screen .brs and .xml files. Confirm:
    - BusySpinner is NOT instantiated in any screen XML (already removed during v1.0 UAT)
    - All screen .brs files set `m.loadingSpinner = invalid`
    - The guard pattern `if m.loadingSpinner <> invalid then m.loadingSpinner.showSpinner = true/false` is used everywhere
    - HomeScreen.xml is in TEST4b state (has Animation nodes, no LoadingSpinner)

    CONFIRM TEST4b STATUS: Check UAT-DEBUG-CONTEXT.md. TEST4b (animations without BusySpinner) was PENDING device test. The current shipped v1.0 app IS TEST4b — it has been running without crashes since v1.0 shipped 2026-03-08. If the app has been working for 5 days with animations and no BusySpinner, TEST4b is confirmed PASSED. Document this conclusion.

    ROOT CAUSE CONCLUSION: BusySpinner (the native Roku `roSGNode` type "BusySpinner") causes SIGSEGV ~3s after init. Animation nodes are safe (confirmed by 5 days of production use). The fix is to never instantiate BusySpinner in the scene graph.

    REPLACE LoadingSpinner.xml: Replace the BusySpinner child with a safe alternative per locked decisions:
    - Full-screen semi-transparent overlay (Rectangle 1920x1080, color 0x00000099) for input blocking
    - Centered "Loading..." Label (translation [960, 530], horizAlign center, font MediumSystemFont, color 0xA0A0B0FF)
    - Keep the `<interface>` field `showSpinner` exactly as-is (all 40+ call sites depend on it)

    UPDATE LoadingSpinner.brs:
    - Keep `m.spinner = m.top.findNode("spinner")` (the Label now has id="spinner")
    - Add `m.overlay = m.top.findNode("overlay")`
    - In `onShowSpinnerChange`: when showing, set both overlay and spinner visible; when hiding, set both invisible
    - Remove `m.spinner.control = "start"/"stop"` lines (Label has no `control` field)
    - Add 300ms delay threshold: record `m.loadStartTime = CreateObject("roTimespan")` when show=true is requested. Use a Timer node (or check elapsed in a deferred callback) — if the data arrives before 300ms, never show the spinner. Simplest approach: set a 300ms Timer; on Timer fire, check if showSpinner is still true, then make overlay+label visible. If showSpinner becomes false before Timer fires, cancel/ignore.

    RE-ENABLE LoadingSpinner IN ALL SCREEN XMLs: Add `<LoadingSpinner id="loadingSpinner" />` back to each screen's XML children. Screens to update: HomeScreen.xml, DetailScreen.xml, EpisodeScreen.xml, PlaylistScreen.xml, SearchScreen.xml, SettingsScreen.xml, UserPickerScreen.xml.

    RE-WIRE SCREEN .BRS FILES: In each screen's `init()`, change `m.loadingSpinner = invalid` to `m.loadingSpinner = m.top.findNode("loadingSpinner")`. The existing guard pattern (`if m.loadingSpinner <> invalid`) will then activate the spinner. Files: HomeScreen.brs, DetailScreen.brs, EpisodeScreen.brs, PlaylistScreen.brs, SearchScreen.brs, SettingsScreen.brs, UserPickerScreen.brs.

    ASSESS VideoPlayer transcodingSpinner: VideoPlayer.xml has `<BusySpinner id="transcodingSpinner">` inside the transcode overlay. This is the same native component that crashes. Replace it with a Label "Switching..." inside the existing transcodingOverlay group. Update VideoPlayer.brs references: remove any `.control = "start"/"stop"` calls on transcodingSpinner, just toggle `.visible`.

    DELETE .bak FILES: Remove HomeScreen.brs.bak and HomeScreen.xml.bak — they are no longer needed since the root cause is confirmed and the fix is in place.

    UPDATE UAT-DEBUG-CONTEXT.md: Mark TEST4b as PASSED (confirmed by production use since 2026-03-08). Mark status as RESOLVED. Document root cause: "BusySpinner native component causes SIGSEGV ~3s after init on Roku hardware. Animation nodes are safe. Fix: replace BusySpinner with Label+Rectangle overlay in LoadingSpinner widget."
  </action>
  <verify>
    <automated>cd "C:/Users/TobyHorton/OneDrive - Owlfarm/Dev/SimPlex" && grep -r "BusySpinner" SimPlex/components/ | grep -v ".bak" | wc -l</automated>
    Expected: 0 (no BusySpinner references remain in any component file)

    Also verify: `grep -r "m.loadingSpinner = invalid" SimPlex/components/screens/*.brs | wc -l` returns 0 (all screens re-wired to findNode)
    Also verify: `grep -r "m.loadingSpinner = m.top.findNode" SimPlex/components/screens/*.brs | wc -l` returns 7 (all 7 screens wired)
  </verify>
  <done>
    - BusySpinner is not instantiated anywhere in the component tree
    - LoadingSpinner widget uses Label+Rectangle overlay with 300ms delay threshold
    - All 7 screens have LoadingSpinner in their XML and wire it in init()
    - VideoPlayer transcodingSpinner replaced with safe Label
    - UAT-DEBUG-CONTEXT.md updated with confirmed root cause
    - .bak files deleted
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned files (normalizers.brs, capabilities.brs)</name>
  <files>
    SimPlex/source/normalizers.brs
    SimPlex/source/capabilities.brs
  </files>
  <action>
    VERIFY BEFORE DELETE: Run grep to confirm no function from either file is called anywhere in the project:
    ```
    grep -r "NormalizeMovieList\|NormalizeShowList\|NormalizeSeasonList\|NormalizeEpisodeList\|NormalizeOnDeck\|ParseServerCapabilities\|HasCapability\|GetMinVersionForFeature\|MeetsMinVersion" SimPlex/
    ```
    Expected: matches only inside normalizers.brs and capabilities.brs themselves. If any other file calls these functions, do NOT delete and document the finding.

    Also verify no XML file includes these as script sources:
    ```
    grep -r "normalizers.brs\|capabilities.brs" SimPlex/components/
    ```
    Expected: zero matches.

    DELETE both files: `SimPlex/source/normalizers.brs` and `SimPlex/source/capabilities.brs`.

    VERIFY COMPILATION: Build the zip to confirm the app packages without errors:
    ```powershell
    cd "C:/Users/TobyHorton/OneDrive - Owlfarm/Dev/SimPlex"
    Remove-Item 'SimPlex.zip' -ErrorAction SilentlyContinue
    Compress-Archive -Path 'SimPlex/manifest','SimPlex/source','SimPlex/components','SimPlex/images' -DestinationPath 'SimPlex.zip' -Force
    ```
    The zip should build successfully (Roku validates at sideload time, but missing source files would cause script errors).
  </action>
  <verify>
    <automated>cd "C:/Users/TobyHorton/OneDrive - Owlfarm/Dev/SimPlex" && test ! -f SimPlex/source/normalizers.brs && test ! -f SimPlex/source/capabilities.brs && echo "PASS: orphaned files deleted" || echo "FAIL: files still exist"</automated>
  </verify>
  <done>
    - normalizers.brs and capabilities.brs are deleted from source/
    - No compilation or runtime errors from their absence
    - Grep confirmed zero call-sites before deletion
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify safe loading indicator on Roku device</name>
  <action>Sideload and verify on device</action>
  <what-built>
    Safe LoadingSpinner replacement (BusySpinner removed, replaced with Label+Rectangle overlay with 300ms delay), LoadingSpinner restored to all 7 screens, VideoPlayer transcodingSpinner replaced, orphaned files deleted, .bak files cleaned up.
  </what-built>
  <how-to-verify>
    1. Build the app: Run the PowerShell build command to create SimPlex.zip
    2. Sideload to Roku via http://{roku-ip}:8060
    3. Launch the app — confirm NO CRASH (the SIGSEGV that was caused by BusySpinner should not occur)
    4. Navigate to a library — if content takes >300ms to load, you should see a "Loading..." overlay; if it loads fast, no spinner flash
    5. Open a show detail screen — same loading behavior
    6. Play a video and switch subtitle tracks — confirm no crash from the replaced transcodingSpinner in VideoPlayer
    7. Navigate back through screens — app remains stable
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (crashes, visual problems, missing loading indicator)</resume-signal>
</task>

</tasks>

<verification>
- `grep -r "BusySpinner" SimPlex/components/ | grep -v ".bak"` returns zero matches
- `grep -r "m.loadingSpinner = invalid" SimPlex/components/screens/*.brs` returns zero matches
- `grep -r "m.loadingSpinner = m.top.findNode" SimPlex/components/screens/*.brs` returns 7 matches
- normalizers.brs and capabilities.brs do not exist
- HomeScreen.brs.bak and HomeScreen.xml.bak do not exist
- SimPlex.zip builds without errors
- App sideloads and runs without SIGSEGV crash
</verification>

<success_criteria>
- BusySpinner SIGSEGV root cause is confirmed and documented in UAT-DEBUG-CONTEXT.md
- All screens use the safe LoadingSpinner (Label+Rectangle, 300ms delay, input blocking)
- VideoPlayer transcodingSpinner replaced with safe Label alternative
- normalizers.brs and capabilities.brs are deleted
- App compiles, sideloads, and runs without crashes
</success_criteria>

<output>
After completion, create `.planning/phases/11-crash-safety-and-foundation/11-01-SUMMARY.md`
</output>
