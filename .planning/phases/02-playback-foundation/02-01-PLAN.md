---
phase: 02-playback-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SimPlex/source/constants.brs
  - SimPlex/images/badge-unwatched.png
  - SimPlex/components/widgets/PosterGridItem.xml
  - SimPlex/components/widgets/PosterGridItem.brs
  - SimPlex/components/widgets/EpisodeItem.xml
  - SimPlex/components/widgets/EpisodeItem.brs
  - SimPlex/components/screens/HomeScreen.brs
  - SimPlex/components/screens/EpisodeScreen.brs
autonomous: true
requirements: [PLAY-02, PLAY-03]

must_haves:
  truths:
    - "Poster grid items show a gold progress bar at the bottom when viewOffset >= 5% of duration"
    - "Poster grid items show no progress bar when viewOffset < 5% of duration"
    - "Poster grid items show a gold corner triangle badge when unwatched (viewCount=0) and no progress bar is showing"
    - "TV show posters show unwatched episode count number inside the triangle badge"
    - "Episode list items show a progress bar on the thumbnail when partially watched"
    - "Episode list items show a triangle badge on the thumbnail when unwatched and no progress bar"
    - "Items with a progress bar do NOT also show the triangle badge (coexistence rule)"
    - "Fully watched items show a clean poster with no badge or progress bar"
  artifacts:
    - path: "SimPlex/components/widgets/PosterGridItem.xml"
      provides: "Progress bar rectangles and triangle badge Poster node"
      contains: "progressTrack"
    - path: "SimPlex/components/widgets/PosterGridItem.brs"
      provides: "Progress bar calculation and badge visibility logic"
      contains: "updateProgressBar"
    - path: "SimPlex/components/widgets/EpisodeItem.xml"
      provides: "Progress bar rectangles and triangle badge on episode thumbnails"
      contains: "progressTrack"
    - path: "SimPlex/components/widgets/EpisodeItem.brs"
      provides: "Progress bar and badge logic for episodes"
      contains: "updateProgressBar"
    - path: "SimPlex/images/badge-unwatched.png"
      provides: "White right-angle triangle PNG for blendColor tinting"
    - path: "SimPlex/source/constants.brs"
      provides: "PROGRESS_BAR_HEIGHT_POSTER, PROGRESS_BAR_HEIGHT_EPISODE, PROGRESS_MIN_PERCENT constants"
  key_links:
    - from: "SimPlex/components/widgets/PosterGridItem.brs"
      to: "m.global.constants"
      via: "ACCENT color for progress fill and badge blendColor"
      pattern: "m\\.global\\.constants"
    - from: "SimPlex/components/screens/HomeScreen.brs"
      to: "SimPlex/components/widgets/PosterGridItem.brs"
      via: "ContentNode fields viewCount, duration, leafCount, viewedLeafCount passed through"
      pattern: "viewCount|leafCount"
---

<objective>
Add progress bar overlays and triangle unwatched badges to PosterGridItem and EpisodeItem components.

Purpose: Users can see at a glance which items are partially watched (progress bar) and which are unwatched (corner triangle badge), matching the official Plex app's visual language.
Output: Updated PosterGridItem and EpisodeItem with progress bars and triangle badges, white triangle PNG asset, updated constants, updated data flow in HomeScreen and EpisodeScreen to pass watch-state fields.
</objective>

<execution_context>
@C:/Users/TobyHorton/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TobyHorton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-playback-foundation/02-CONTEXT.md
@.planning/phases/02-playback-foundation/02-RESEARCH.md
@SimPlex/components/widgets/PosterGridItem.xml
@SimPlex/components/widgets/PosterGridItem.brs
@SimPlex/components/widgets/EpisodeItem.xml
@SimPlex/components/widgets/EpisodeItem.brs
@SimPlex/components/screens/HomeScreen.brs
@SimPlex/components/screens/EpisodeScreen.brs
@SimPlex/source/constants.brs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create triangle PNG asset and add watch-state constants</name>
  <files>
    SimPlex/images/badge-unwatched.png
    SimPlex/source/constants.brs
  </files>
  <action>
1. Create a white right-angle triangle PNG image at `SimPlex/images/badge-unwatched.png`:
   - Size: 40x40 pixels
   - Shape: Right-angle triangle occupying the top-right corner (vertices at top-left, top-right, bottom-right)
   - Color: Pure white (#FFFFFF) with full opacity in the triangle area
   - Background: Fully transparent (alpha=0) in the non-triangle area
   - Format: PNG-24 with alpha channel
   - This will be tinted at runtime via Poster.blendColor to match the accent color, enabling future theme support per user decision.
   - Use a script or ImageMagick to generate this. Example: `magick -size 40x40 xc:none -fill white -draw "polygon 0,0 40,0 40,40" PNG32:badge-unwatched.png`

2. Update `SimPlex/source/constants.brs` GetConstants() to add these new constants:
   ```brightscript
   ' Watch State UI
   PROGRESS_BAR_HEIGHT_POSTER: 6        ' pixels, progress bar on poster grid items (240x360)
   PROGRESS_BAR_HEIGHT_EPISODE: 4       ' pixels, progress bar on episode thumbnails (213x120)
   PROGRESS_BAR_HEIGHT_DETAIL: 8        ' pixels, progress bar on detail screen
   PROGRESS_MIN_PERCENT: 0.05           ' below 5% = treat as not started
   BADGE_SIZE: 40                       ' pixels, triangle badge dimensions
   BADGE_SIZE_EPISODE: 28               ' pixels, smaller badge for episode thumbnails
   ```
   Add these inside the existing return object, in a new "Watch State UI" section after the existing "Layout" section.
  </action>
  <verify>
    - File `SimPlex/images/badge-unwatched.png` exists and is a valid PNG
    - `SimPlex/source/constants.brs` contains PROGRESS_BAR_HEIGHT_POSTER, PROGRESS_MIN_PERCENT, BADGE_SIZE constants
    - BrighterScript compiles: `npx bsc --project bsconfig.json` exits with 0 errors
  </verify>
  <done>White triangle PNG asset exists. Constants file has all watch-state UI constants. Project compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add progress bars and triangle badges to PosterGridItem and EpisodeItem</name>
  <files>
    SimPlex/components/widgets/PosterGridItem.xml
    SimPlex/components/widgets/PosterGridItem.brs
    SimPlex/components/widgets/EpisodeItem.xml
    SimPlex/components/widgets/EpisodeItem.brs
    SimPlex/components/screens/HomeScreen.brs
    SimPlex/components/screens/EpisodeScreen.brs
  </files>
  <action>
**PosterGridItem.xml changes:**
- Remove the existing `unwatchedIndicator` Rectangle (the 12x12 dot at [220,10])
- Add AFTER the Poster node (z-order: must be on top of poster):
  - `progressTrack` Rectangle: translation=[0,354], width=240, height=6, color="0xFFFFFF33", visible=false
  - `progressFill` Rectangle: translation=[0,354], width=0, height=6, color="0xE5A00DFF", visible=false
  - `unwatchedBadge` Poster: translation=[200,0], width=40, height=40, uri="pkg:/images/badge-unwatched.png", visible=false
  - `unwatchedCount` Label: translation=[218,4], font="font:SmallestSystemFont", color="0xFFFFFFFF", visible=false, horizAlign="right", width=18

**PosterGridItem.brs changes:**
- In init(): find the new nodes (progressTrack, progressFill, unwatchedBadge, unwatchedCount). Remove m.unwatchedIndicator reference. Cache constants: `m.constants = m.global.constants`. Set badge blendColor: `m.unwatchedBadge.blendColor = m.constants.ACCENT`
- In onItemContentChange(): Replace the existing unwatched indicator logic with calls to two new subs:
  - Call `updateProgressBar(content)` and `updateBadge(content)`
  - The content node now needs these fields: viewOffset, duration, viewCount (integer), itemType (string), leafCount (integer), viewedLeafCount (integer)
- New sub `updateProgressBar(content as Object)`:
  - If content.duration = invalid or content.duration <= 0 or content.viewOffset = invalid or content.viewOffset <= 0: hide both progress rectangles, return
  - Calculate progress = content.viewOffset / content.duration (as Float)
  - If progress < m.constants.PROGRESS_MIN_PERCENT (0.05): hide both, return
  - Show both rectangles. Set progressFill.width = Int(240 * progress). Set progressFill.color = m.constants.ACCENT
- New sub `updateBadge(content as Object)`:
  - If progress bar is visible (m.progressTrack.visible = true): hide badge and count label, return (coexistence rule)
  - Determine watched state: if content.viewCount <> invalid and content.viewCount > 0 then watched = true, else watched = false
  - If watched: hide badge and count, return (clean poster for watched items)
  - If unwatched: show badge
  - If content.itemType = "show" and content.leafCount <> invalid and content.viewedLeafCount <> invalid:
    - unwatchedEpisodes = content.leafCount - content.viewedLeafCount
    - If unwatchedEpisodes <= 0: hide badge (fully watched show), return
    - Show count label with unwatchedEpisodes.ToStr()
  - Else: hide count label (movies and individual episodes = triangle only, no count)

**EpisodeItem.xml changes:**
- Remove the existing `watchedIndicator` Rectangle (the 12x12 dot at [195,5])
- Add AFTER the thumbnail Poster node:
  - `progressTrack` Rectangle: translation=[0,116], width=213, height=4, color="0xFFFFFF33", visible=false
  - `progressFill` Rectangle: translation=[0,116], width=0, height=4, color="0xE5A00DFF", visible=false
  - `unwatchedBadge` Poster: translation=[185,0], width=28, height=28, uri="pkg:/images/badge-unwatched.png", visible=false

**EpisodeItem.brs changes:**
- In init(): find new nodes (progressTrack, progressFill, unwatchedBadge). Remove m.watchedIndicator. Cache constants. Set badge blendColor.
- In onItemContentChange(): Replace watched indicator logic with:
  - Call updateProgressBar(content) and updateBadge(content)
  - Content node already has viewOffset, duration, watched fields from EpisodeScreen
- New sub `updateProgressBar(content as Object)`: Same logic as PosterGridItem but with width=213 and constants for episode bar height
- New sub `updateBadge(content as Object)`:
  - If progress bar visible: hide badge, return
  - If content.watched = true (or content.viewCount > 0): hide badge, return
  - Else: show badge (no count label for individual episodes per user decision)

**HomeScreen.brs changes** (data flow fix):
- In processApiResponse(), when building content nodes, add these fields from the API response:
  - `viewCount: 0` (default), then `if item.viewCount <> invalid then node.viewCount = item.viewCount`
  - `duration: 0` (default), then `if item.duration <> invalid then node.duration = item.duration`
  - `leafCount: 0` and `viewedLeafCount: 0` (defaults), then populate from item if present
  - `itemType` is already being set as `item.type` -- confirm this is present

**EpisodeScreen.brs changes** (data flow fix):
- In processEpisodes(), the content nodes already have viewOffset, duration, and watched fields. Also add `viewCount: 0` as a field, and set it: `if episode.viewCount <> invalid then node.viewCount = episode.viewCount`. This allows the badge logic to use viewCount consistently.

**Critical notes:**
- All accent-colored elements MUST reference m.global.constants.ACCENT, not hardcoded "0xE5A00DFF", per user decision for future theme support
- Progress bar visible regardless of focus state per user decision
- Below 5% progress = no bar (treat as not started) per user decision
- Show bar up to 100% -- do NOT auto-mark watched per user decision
  </action>
  <verify>
    - BrighterScript compiles: `npx bsc --project bsconfig.json` exits with 0 errors
    - PosterGridItem.xml contains progressTrack, progressFill, unwatchedBadge nodes
    - PosterGridItem.brs contains updateProgressBar and updateBadge subs
    - EpisodeItem.xml contains progressTrack, progressFill, unwatchedBadge nodes
    - EpisodeItem.brs contains updateProgressBar and updateBadge subs
    - No hardcoded "0xE5A00DFF" in PosterGridItem.brs or EpisodeItem.brs (all accent colors via constants)
    - HomeScreen.brs passes viewCount, duration, leafCount, viewedLeafCount to content nodes
  </verify>
  <done>
    PosterGridItem shows gold progress bar at bottom when partially watched (>=5%), gold triangle badge in top-right corner when unwatched (with episode count for shows), and clean poster when fully watched. EpisodeItem shows same patterns on thumbnails. Progress bar and badge never appear simultaneously. All accent colors reference m.global.constants.ACCENT for future theme support.
  </done>
</task>

</tasks>

<verification>
- BrighterScript compilation passes with zero errors
- PosterGridItem has progress bar (2 rectangles) and triangle badge (Poster + Label) declared AFTER the main Poster node in XML
- EpisodeItem has progress bar and triangle badge declared AFTER the thumbnail Poster node in XML
- Old dot indicators (unwatchedIndicator, watchedIndicator) are removed
- All accent colors use m.global.constants.ACCENT, not hardcoded values
- Content nodes in HomeScreen and EpisodeScreen include viewCount, duration, leafCount, viewedLeafCount fields
- Badge visibility respects coexistence rule: progress bar showing = badge hidden
- 5% minimum threshold enforced in updateProgressBar
</verification>

<success_criteria>
- Grid items visually distinguish: partially watched (progress bar), unwatched (triangle badge with count for shows), and fully watched (clean poster)
- Episode items visually distinguish: partially watched, unwatched, and watched
- All visual elements are theme-aware via constants
- Project compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-playback-foundation/02-01-SUMMARY.md`
</output>
