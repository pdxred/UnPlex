---
phase: 02-playback-foundation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - SimPlex/components/screens/HomeScreen.brs
  - SimPlex/components/screens/HomeScreen.xml
  - SimPlex/components/screens/EpisodeScreen.brs
  - SimPlex/components/screens/EpisodeScreen.xml
  - SimPlex/components/screens/DetailScreen.brs
  - SimPlex/components/screens/DetailScreen.xml
  - SimPlex/components/widgets/PosterGrid.brs
  - SimPlex/components/widgets/PosterGrid.xml
autonomous: true
requirements: [PLAY-01, PLAY-04, PLAY-05]

must_haves:
  truths:
    - "Selecting a partially-watched item from the grid shows a resume dialog with 'Resume from X:XX' and 'Start from Beginning' buttons"
    - "Selecting a partially-watched item from the episode list shows the same resume dialog"
    - "Detail screen shows separate 'Resume from X:XX' and 'Play' buttons (no dialog) for partially-watched items"
    - "Detail screen shows 'X min remaining' text alongside a progress bar for partially-watched items"
    - "User can press options (*) key on a focused grid item to see Mark Watched/Unwatched context menu"
    - "User can mark an item as watched from detail screen and the badge updates immediately"
    - "User can mark an item as unwatched from detail screen and the badge updates immediately"
    - "User can mark an item as watched from the options context menu on the grid"
    - "TV show mark-watched scopes to the whole show; season scopes to whole season"
  artifacts:
    - path: "SimPlex/components/screens/DetailScreen.brs"
      provides: "Resume buttons, remaining time text, optimistic mark watched/unwatched"
      contains: "getRemainingTimeText"
    - path: "SimPlex/components/screens/DetailScreen.xml"
      provides: "Progress bar and remaining time label on detail screen"
      contains: "remainingLabel"
    - path: "SimPlex/components/screens/HomeScreen.brs"
      provides: "Resume dialog on grid selection, options key context menu"
      contains: "showResumeDialog"
    - path: "SimPlex/components/screens/EpisodeScreen.brs"
      provides: "Resume dialog on episode selection, options key context menu"
      contains: "showResumeDialog"
  key_links:
    - from: "SimPlex/components/screens/HomeScreen.brs"
      to: "StandardMessageDialog"
      via: "Resume dialog on grid item selection when viewOffset > 0"
      pattern: "showResumeDialog"
    - from: "SimPlex/components/screens/HomeScreen.brs"
      to: "/:/scrobble"
      via: "Options key context menu triggers mark watched API call"
      pattern: "markItemWatched|scrobble"
    - from: "SimPlex/components/screens/DetailScreen.brs"
      to: "SimPlex/components/widgets/PosterGridItem.brs"
      via: "After mark watched/unwatched, updates ContentNode in-place to refresh badge"
      pattern: "optimistic|viewCount"
---

<objective>
Add resume playback dialog, detail screen enhancements (progress bar + remaining time), options key context menu for mark watched/unwatched, and optimistic UI updates.

Purpose: Users can resume playback with a single dialog from grids, see remaining time on detail screens, and quickly mark items watched/unwatched via the options remote key -- all with instant visual feedback.
Output: Resume dialog on grid/episode selection, detail screen progress bar with "X min remaining", options (*) key context menu on grids and episode lists, optimistic badge/progress bar updates after state changes.
</objective>

<execution_context>
@C:/Users/TobyHorton/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TobyHorton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-playback-foundation/02-CONTEXT.md
@.planning/phases/02-playback-foundation/02-RESEARCH.md
@.planning/phases/02-playback-foundation/02-01-SUMMARY.md
@SimPlex/components/screens/HomeScreen.brs
@SimPlex/components/screens/HomeScreen.xml
@SimPlex/components/screens/EpisodeScreen.brs
@SimPlex/components/screens/EpisodeScreen.xml
@SimPlex/components/screens/DetailScreen.brs
@SimPlex/components/screens/DetailScreen.xml
@SimPlex/components/widgets/PosterGrid.brs
@SimPlex/components/widgets/PosterGrid.xml
@SimPlex/source/utils.brs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resume dialog to HomeScreen and EpisodeScreen, plus options key context menu</name>
  <files>
    SimPlex/components/screens/HomeScreen.brs
    SimPlex/components/screens/HomeScreen.xml
    SimPlex/components/screens/EpisodeScreen.brs
    SimPlex/components/screens/EpisodeScreen.xml
    SimPlex/components/widgets/PosterGrid.brs
    SimPlex/components/widgets/PosterGrid.xml
  </files>
  <action>
**HomeScreen.brs - Resume Dialog:**
- Modify onGridItemSelected(): Before navigating to detail screen, check if the selected content node has viewOffset > 0 AND duration > 0 AND (viewOffset / duration) >= 0.05 (using PROGRESS_MIN_PERCENT from constants). If so, call showResumeDialog(item) instead of navigating to detail. If not partially watched, navigate to detail as before.
- New sub showResumeDialog(item as Object):
  - Store m.pendingPlayItem = item (the ContentNode)
  - Store m.pendingFocusTarget = m.posterGrid (to restore focus after dialog dismiss)
  - Create StandardMessageDialog:
    - title = item.title
    - resumeTime = FormatTime(item.viewOffset) (FormatTime is in utils.brs, included via script tag)
    - message = ["Resume from " + resumeTime + "?"]
    - buttons = ["Resume from " + resumeTime, "Start from Beginning", "Go to Details"]
    - Observe buttonSelected -> onResumeDialogButton
    - Observe wasClosed -> onResumeDialogClosed (for back-button dismiss, restore focus)
    - Set m.top.getScene().dialog = dialog
- New sub onResumeDialogButton(event as Object):
  - index = event.getData()
  - Close dialog: m.top.getScene().dialog.close = true
  - If index = 0: start playback with resume (create VideoPlayer with startOffset = m.pendingPlayItem.viewOffset)
  - If index = 1: start playback from beginning (startOffset = 0)
  - If index = 2: navigate to detail screen as normal (m.top.itemSelected = { action: "detail", ratingKey: ..., itemType: ... })
- New sub onResumeDialogClosed(event as Object): restore focus to m.pendingFocusTarget
- New sub startPlaybackFromGrid(item as Object, offset as Integer):
  - Create VideoPlayer node, set ratingKey, mediaKey, startOffset, itemTitle
  - Observe playbackComplete -> onGridPlaybackComplete
  - Append to scene, set focus, control = "play"
  - Store m.player = player
- New sub onGridPlaybackComplete(event as Object):
  - Remove player from scene, restore focus to grid
  - Optionally refresh the current library to update watch states (call loadLibrary() if m.currentOffset > 0, or just update the specific content node optimistically)

**HomeScreen - Options Key Context Menu:**
- In onKeyEvent(): Add handler for key = "options":
  - Get the currently focused grid item index from m.posterGrid (check if posterGrid has a focusedIndex or itemFocused field -- PosterGrid likely exposes this)
  - If valid item focused, call showOptionsMenu(item)
  - Return true to consume the key
- New sub showOptionsMenu(item as Object):
  - Store m.pendingOptionsItem = item
  - Store m.pendingOptionsIndex = focusedIndex
  - Create StandardMessageDialog (using dialog for MVP, per research recommendation):
    - title = item.title (truncated if needed)
    - Determine label based on item type and watched state:
      - If item.viewCount > 0: button = "Mark as Unwatched"
      - Else: button = "Mark as Watched"
      - If item.itemType = "show": prefix with "Mark Show as "
    - buttons = [watchedLabel, "Cancel"]
    - Observe buttonSelected -> onOptionsMenuButton
    - Observe wasClosed -> onOptionsMenuClosed
    - Set m.top.getScene().dialog = dialog
- New sub onOptionsMenuButton(event as Object):
  - index = event.getData()
  - Close dialog
  - If index = 0: toggle watched state
    - Optimistic update: modify the ContentNode directly (item.viewCount = toggled value, item.viewOffset = 0 if marking watched)
    - The PosterGridItem component will re-render via onItemContentChange if we re-assign the content node. To trigger re-render: get the parent content, remove the child at the index, create a new child with same data, insert at same index. OR simpler: re-set m.posterGrid.content = m.posterGrid.content (force re-render).
    - Fire PlexApiTask with /:/scrobble or /:/unscrobble endpoint
  - If index = 1: cancel, just restore focus
- New sub onOptionsMenuClosed(event as Object): restore focus to grid

**HomeScreen.xml changes:**
- Ensure `<script type="text/brightscript" uri="pkg:/source/utils.brs" />` is included (for FormatTime function). Check if already present.

**PosterGrid.xml/brs changes:**
- Need to expose the currently focused item index. Check if PosterGrid already has an itemFocused field or similar. If using MarkupGrid internally, MarkupGrid has itemFocused field. Add interface field `focusedIndex` if needed, or use the MarkupGrid's itemFocused directly from HomeScreen via m.posterGrid.findNode("markupGrid").itemFocused.

**EpisodeScreen.brs - Resume Dialog:**
- Modify onEpisodeSelected(): Same pattern as HomeScreen -- check if episode has viewOffset > 0 with >= 5% progress, show resume dialog instead of immediate playback
- Add showResumeDialog, onResumeDialogButton, onResumeDialogClosed subs (same pattern as HomeScreen but using episode data)
- startPlayback already exists -- modify it to accept an offset parameter (it currently always uses episode.viewOffset). Change signature or add a wrapper.

**EpisodeScreen.brs - Options Key Context Menu:**
- In onKeyEvent(): Add options key handler when focus is on episode list
- Show options menu for focused episode with Mark Watched/Unwatched
- Optimistic update: modify the episode ContentNode's watched and viewCount fields, then trigger re-render
- Fire scrobble/unscrobble API call

**EpisodeScreen.xml changes:**
- Ensure utils.brs script is included for FormatTime.

**Critical implementation notes:**
- Resume dialog is ONLY for grid/episode list selections, NOT detail screen (per user locked decision)
- Dialog must include "Go to Details" option so user can still navigate to full detail screen
- FormatTime function exists in utils.brs AND in EpisodeItem.brs locally. Use the utils.brs version (ensure script tag is present). The EpisodeItem.brs local copy should be kept since EpisodeItem may not include utils.brs.
- After dialog close, ALWAYS restore focus explicitly to prevent focus trap (pitfall from research)
- Use create-per-request pattern for PlexApiTask (Phase 1 established pattern)
- Context-aware scoping: show ratingKey marks whole show, season ratingKey marks whole season, episode ratingKey marks that episode. The Plex API handles this automatically when you pass the appropriate ratingKey.
  </action>
  <verify>
    - BrighterScript compiles: `npx bsc --project bsconfig.json` exits with 0 errors
    - HomeScreen.brs contains showResumeDialog, onResumeDialogButton, showOptionsMenu subs
    - EpisodeScreen.brs contains showResumeDialog, onResumeDialogButton subs
    - HomeScreen.brs onKeyEvent handles "options" key
    - EpisodeScreen.brs onKeyEvent handles "options" key
    - No direct call to GetConstants() in any modified file (use m.global.constants)
  </verify>
  <done>
    Grid item selection for partially-watched items shows resume dialog with Resume/Start from Beginning/Go to Details. Episode selection for partially-watched items shows same resume dialog. Options (*) key on grid or episode list shows context menu with Mark Watched/Unwatched. Optimistic UI updates change badge/progress bar immediately. Focus is properly restored after all dialog interactions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance DetailScreen with progress bar, remaining time, and optimistic mark watched/unwatched</name>
  <files>
    SimPlex/components/screens/DetailScreen.xml
    SimPlex/components/screens/DetailScreen.brs
  </files>
  <action>
**DetailScreen.xml changes:**
- Add a progress section below the metadata group (or within it), before the ButtonGroup:
  - `progressGroup` Group: translation=[540, 700] (adjust to fit between summary and buttons)
    - `detailProgressTrack` Rectangle: width=400, height=8, color="0xFFFFFF33", visible=false
    - `detailProgressFill` Rectangle: width=0, height=8, color="0xE5A00DFF", visible=false
    - `remainingLabel` Label: translation=[0, 16], font="font:SmallSystemFont", color="0xA0A0B0FF", visible=false, width=400
  - Adjust ButtonGroup translation to accommodate the progress section (move down ~40px to [540, 760] or similar)

**DetailScreen.brs changes:**

1. In init(): find new nodes (detailProgressTrack, detailProgressFill, remainingLabel). Cache constants.

2. In processMetadata(): After storing m.viewOffset, add:
   - Call updateDetailProgress() to show progress bar and remaining time
   - The progress bar width should be proportional to the metadata area (use 400px width to match poster width, or wider to match the metadata column)

3. New sub updateDetailProgress():
   - If m.itemData.duration = invalid or m.itemData.duration <= 0 or m.viewOffset <= 0: hide progress and remaining, return
   - progress = m.viewOffset / m.itemData.duration (as Float)
   - If progress < m.constants.PROGRESS_MIN_PERCENT: hide progress and remaining, return
   - Show progress track and fill. Set fill width = Int(400 * progress). Set fill color = m.constants.ACCENT
   - Calculate and show remaining time text:
     - remainingMs = m.itemData.duration - m.viewOffset
     - remainingMin = remainingMs \ 60000
     - If remainingMin < 1: m.remainingLabel.text = "Less than 1 min remaining"
     - Else: m.remainingLabel.text = remainingMin.ToStr() + " min remaining"
     - m.remainingLabel.visible = true

4. In buildButtons(): The existing logic already shows "Resume from X:XX" as a separate button when viewOffset > 0. This is correct per user decision (detail screen uses separate buttons, not dialog). Reorder buttons so "Resume" appears FIRST (before "Play"):
   - If viewOffset > 0:
     - First button: "Resume from " + FormatTime(m.viewOffset) with action "resume"
     - Second button: "Play from Beginning" with action "play" (rename from just "Play" for clarity)
   - Else:
     - Single button: "Play" with action "play"
   - Keep Mark Watched/Unwatched button after play buttons

5. Enhance markAsWatched() and markAsUnwatched() with optimistic updates:
   - markAsWatched():
     - Immediately update m.itemData.viewCount = 1, m.viewOffset = 0, m.itemData.viewOffset = 0
     - Hide progress bar and remaining time
     - Rebuild buttons (will now show "Play" instead of "Resume")
     - Then fire the API task (existing code)
   - markAsUnwatched():
     - Immediately update m.itemData.viewCount = 0
     - Rebuild buttons (will now show "Mark as Watched" instead of "Mark as Unwatched")
     - Then fire the API task (existing code)
   - In onWatchedStateChange(): On error, show a brief error toast. On success, do nothing (optimistic update already applied). Remove the existing loadMetadata() refresh call since we're doing optimistic updates now.

6. Add a new interface field to DetailScreen for propagating watch state changes back to the parent screen:
   - In DetailScreen.xml interface: add `<field id="watchStateChanged" type="assocarray" alwaysNotify="true" />`
   - After optimistic update in markAsWatched/markAsUnwatched, set: `m.top.watchStateChanged = { ratingKey: m.itemData.ratingKey, viewCount: m.itemData.viewCount, viewOffset: m.viewOffset }`
   - This allows HomeScreen/MainScene to update the grid item's ContentNode when returning from detail screen

**Critical notes:**
- Detail screen uses SEPARATE BUTTONS for resume, NOT a dialog (locked user decision)
- "Resume from X:XX" button should appear first/top when item is partially watched
- Progress bar color MUST use m.constants.ACCENT (theme-aware)
- Optimistic updates: change UI immediately, fire API in background, only revert on error (per research recommendation and user preference for speed)
- The existing onPlaybackComplete already calls loadMetadata() to refresh after playback -- keep this behavior since playback may change viewOffset/viewCount
  </action>
  <verify>
    - BrighterScript compiles: `npx bsc --project bsconfig.json` exits with 0 errors
    - DetailScreen.xml contains detailProgressTrack, detailProgressFill, remainingLabel nodes
    - DetailScreen.brs contains updateDetailProgress sub
    - DetailScreen.brs markAsWatched has optimistic update (sets viewCount before API call)
    - DetailScreen.xml interface has watchStateChanged field
    - Resume button appears before Play button when viewOffset > 0
    - No hardcoded accent colors in DetailScreen.brs
  </verify>
  <done>
    Detail screen shows progress bar with "X min remaining" text for partially-watched items. Resume button appears first, Play from Beginning second. Mark Watched/Unwatched updates UI optimistically (instant feedback). Watch state changes are propagated via watchStateChanged interface field for parent screen grid updates.
  </done>
</task>

</tasks>

<verification>
- BrighterScript compilation passes with zero errors
- Resume dialog appears on grid/episode selection for partially-watched items (not on detail screen)
- Detail screen shows separate Resume and Play buttons (not dialog) for partially-watched items
- Detail screen shows progress bar with remaining time text
- Options (*) key shows context menu on grid and episode list screens
- Mark Watched/Unwatched updates visual state immediately (optimistic)
- Watch state changes propagate between detail and grid screens
- All accent colors reference m.global.constants.ACCENT
</verification>

<success_criteria>
- Partially-watched grid item selection triggers resume dialog with Resume/Start/Details options
- Detail screen shows "X min remaining" with progress bar for partially-watched items
- Options key context menu allows quick mark watched/unwatched from grids
- All watch state UI changes are instant (optimistic updates)
- Focus properly restored after every dialog interaction
</success_criteria>

<output>
After completion, create `.planning/phases/02-playback-foundation/02-02-SUMMARY.md`
</output>
