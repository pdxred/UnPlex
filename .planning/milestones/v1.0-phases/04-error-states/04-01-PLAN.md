---
phase: 04-error-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SimPlex/components/widgets/LoadingSpinner.xml
  - SimPlex/components/widgets/LoadingSpinner.brs
  - SimPlex/images/spinner.png
  - SimPlex/components/screens/HomeScreen.xml
  - SimPlex/components/screens/HomeScreen.brs
  - SimPlex/components/screens/EpisodeScreen.xml
  - SimPlex/components/screens/EpisodeScreen.brs
  - SimPlex/components/screens/SearchScreen.xml
  - SimPlex/components/screens/SearchScreen.brs
  - SimPlex/components/screens/DetailScreen.xml
  - SimPlex/components/screens/DetailScreen.brs
autonomous: true
requirements:
  - ERR-01
  - ERR-02

must_haves:
  truths:
    - "Every screen that fetches data shows an animated spinner centered in the content area until content appears"
    - "Empty libraries display a friendly message with guidance instead of a blank screen"
    - "Empty search results display a 'No results' message instead of a blank screen"
    - "Hub rows with no content are hidden entirely (already done, must not regress)"
  artifacts:
    - path: "SimPlex/components/widgets/LoadingSpinner.xml"
      provides: "BusySpinner-based animated loading indicator"
      contains: "BusySpinner"
    - path: "SimPlex/images/spinner.png"
      provides: "64x64 white spinner arc image for BusySpinner rotation"
    - path: "SimPlex/components/screens/HomeScreen.xml"
      provides: "Empty state group for library grid"
      contains: "emptyState"
    - path: "SimPlex/components/screens/EpisodeScreen.xml"
      provides: "Empty state group for episodes"
      contains: "emptyState"
    - path: "SimPlex/components/screens/SearchScreen.xml"
      provides: "Empty state group for search results"
      contains: "emptyState"
  key_links:
    - from: "SimPlex/components/widgets/LoadingSpinner.brs"
      to: "BusySpinner control"
      via: "onVisibleChange sets control start/stop"
      pattern: "control.*=.*start|stop"
    - from: "SimPlex/components/screens/HomeScreen.brs"
      to: "emptyState group"
      via: "Shows empty state when API returns zero items"
      pattern: "emptyState.*visible.*=.*true"
---

<objective>
Upgrade the LoadingSpinner component from a static "Loading..." label to an animated BusySpinner, and add empty state messaging to all screens that can have zero content.

Purpose: ERR-01 requires every async screen to show a spinner; ERR-02 requires empty libraries and searches to display helpful messages instead of blank screens.
Output: Upgraded LoadingSpinner component, spinner PNG asset, empty state UI groups on HomeScreen, EpisodeScreen, SearchScreen, and DetailScreen.
</objective>

<execution_context>
@C:/Users/TobyHorton/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TobyHorton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-error-states/04-CONTEXT.md
@.planning/phases/04-error-states/04-RESEARCH.md

@SimPlex/components/widgets/LoadingSpinner.xml
@SimPlex/components/widgets/LoadingSpinner.brs
@SimPlex/components/screens/HomeScreen.xml
@SimPlex/components/screens/HomeScreen.brs
@SimPlex/components/screens/EpisodeScreen.xml
@SimPlex/components/screens/EpisodeScreen.brs
@SimPlex/components/screens/SearchScreen.xml
@SimPlex/components/screens/SearchScreen.brs
@SimPlex/components/screens/DetailScreen.xml
@SimPlex/components/screens/DetailScreen.brs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade LoadingSpinner to animated BusySpinner</name>
  <files>
    SimPlex/components/widgets/LoadingSpinner.xml
    SimPlex/components/widgets/LoadingSpinner.brs
    SimPlex/images/spinner.png
  </files>
  <action>
1. Create a 64x64 white circular arc/ring PNG image at `SimPlex/images/spinner.png`. This is the graphic BusySpinner rotates. Use a simple white ring with a gap (like a loading arc) on transparent background. Generate this programmatically or use a minimal base64-encoded PNG.

2. Rewrite `LoadingSpinner.xml`: Replace the Label child with a BusySpinner node. The BusySpinner should contain a Poster child pointing to `pkg:/images/spinner.png` with width=64 height=64. Keep the existing interface field `visible` with `onChange="onVisibleChange"`. Remove the Label entirely.

3. Rewrite `LoadingSpinner.brs`:
   - In `init()`: find the BusySpinner node by id, store as `m.spinner`
   - In `onVisibleChange()`: when visible=true, set `m.spinner.visible = true` and `m.spinner.control = "start"`. When visible=false, set `m.spinner.control = "stop"` and `m.spinner.visible = false`.

4. Per user decision: spinner is centered in the content area (right of sidebar, not full screen center). The positioning is handled by each screen's placement of the LoadingSpinner component, not by the component itself. Verify each screen's existing LoadingSpinner placement is approximately centered in the content area (X ~788 relative to content group, Y ~508 for vertical center). Adjust if any screen has it positioned at full-screen center instead.

Per user decision: No text label on the spinner. No minimum display time -- if data arrives instantly, spinner never shows or flashes briefly (that is acceptable).
  </action>
  <verify>
Build with `npx bsc` from repo root. Verify no compilation errors. Visually confirm LoadingSpinner.xml contains BusySpinner (not Label) and references spinner.png. Confirm spinner.png exists and is a valid PNG file.
  </verify>
  <done>LoadingSpinner component uses BusySpinner with animated rotation instead of static "Loading..." text. All existing screens that reference LoadingSpinner continue to work without changes to their code (interface field unchanged).</done>
</task>

<task type="auto">
  <name>Task 2: Add empty state messaging to all content screens</name>
  <files>
    SimPlex/components/screens/HomeScreen.xml
    SimPlex/components/screens/HomeScreen.brs
    SimPlex/components/screens/EpisodeScreen.xml
    SimPlex/components/screens/EpisodeScreen.brs
    SimPlex/components/screens/SearchScreen.xml
    SimPlex/components/screens/SearchScreen.brs
    SimPlex/components/screens/DetailScreen.xml
    SimPlex/components/screens/DetailScreen.brs
  </files>
  <action>
For each screen below, add an `emptyState` Group to the XML and show/hide logic in the BRS:

**HomeScreen (library grid view):**
- Add a Group `id="emptyState"` visible=false, positioned center of content area
- Title Label: "Nothing here yet" (font:LargeBoldSystemFont, color white)
- Message Label below: "Add some content to your Plex library to see it here" (font:MediumSystemFont, color 0xA0A0B0FF, wrap=true, width=600, horizAlign=center)
- In BRS: When API returns successfully but item count is 0, hide the grid/RowList, hide spinner, show emptyState. When items exist, hide emptyState, show grid.
- Per user decision: Hub rows with no content should be hidden entirely (this is already implemented -- do NOT add empty state messages for hub rows, only for library grid browsing)

**EpisodeScreen:**
- Add `emptyState` Group with message "No episodes found" / "This season doesn't have any episodes yet"
- Show when season or episode API returns zero items

**SearchScreen:**
- Add `emptyState` Group with message "No results found" (per user decision: simple message, no suggestion to broaden search)
- Show when search completes with zero results. Hide when new search begins or results arrive.
- Note: Only show after a search has been performed. On initial screen load (no query yet), show nothing (not the empty state).

**DetailScreen:**
- DetailScreen always shows one item so it doesn't need an empty state. However, ensure the spinner hides properly on successful load (verify existing behavior, fix if needed).

Per user decision on visuals: No icons or illustrations above empty state text. Text only. This is consistent with the app's clean aesthetic and avoids PNG asset overhead.

Empty state text colors: Title in white (0xFFFFFFFF), subtitle/message in muted gray (0xA0A0B0FF). Use system fonts.
  </action>
  <verify>
Build with `npx bsc` from repo root. Verify no compilation errors. Grep each modified XML file for "emptyState" to confirm the group exists. Grep each modified BRS file for "emptyState" to confirm show/hide logic exists.
  </verify>
  <done>Empty libraries show "Nothing here yet" with guidance. Empty search results show "No results found". Empty episode lists show appropriate message. Hub rows with no content remain hidden (no regression). No empty state has icons/illustrations.</done>
</task>

</tasks>

<verification>
1. `npx bsc` compiles without errors
2. LoadingSpinner.xml contains BusySpinner node, not Label
3. spinner.png exists at SimPlex/images/spinner.png
4. All four screen XMLs contain emptyState groups
5. All four screen BRS files have emptyState show/hide logic
6. No reference to "Loading..." text remains in LoadingSpinner
</verification>

<success_criteria>
- Animated spinner replaces static "Loading..." text across all screens
- Empty libraries, searches, and episode lists show friendly messages
- Hub rows remain hidden when empty (no regression)
- All changes compile cleanly with BrighterScript
</success_criteria>

<output>
After completion, create `.planning/phases/04-error-states/04-01-SUMMARY.md`
</output>
