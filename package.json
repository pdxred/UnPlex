{
  "name": "simplex",
  "version": "1.0.0",
  "private": true,
  "description": "SimPlex - Custom Plex client for Roku",
  "scripts": {
    "build": "bsc",
    "deploy": "bsc --deploy",
    "lint": "bsc --noEmit"
  },
  "devDependencies": {
    "brighterscript": "^0.70.3",
    "roku-deploy": "^3.16.1"
  }
}
