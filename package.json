{
  "name": "unplex",
  "version": "1.3.0",
  "private": true,
  "description": "UnPlex - Custom Plex client for Roku",
  "scripts": {
    "build": "bsc",
    "deploy": "bsc --deploy",
    "lint": "bsc --noEmit"
  },
  "devDependencies": {
    "brighterscript": "^0.72.2",
    "roku-deploy": "^3.17.6"
  }
}
