{
  "name": "unplex",
  "version": "1.1.0",
  "private": true,
  "description": "UnPlex - Custom Plex client for Roku",
  "scripts": {
    "build": "bsc",
    "deploy": "bsc --deploy",
    "lint": "bsc --noEmit",
    "package": "bsc --out-file ./UnPlex-v1.3.0.zip"
  },
  "devDependencies": {
    "brighterscript": "^0.70.3",
    "roku-deploy": "^3.16.1"
  },
  "dependencies": {
    "install": "^0.13.0",
    "pnpm": "^10.33.0",
    "yarn": "^1.22.22"
  }
}
